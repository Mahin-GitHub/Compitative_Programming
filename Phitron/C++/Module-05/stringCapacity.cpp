#include <bits/stdc++.h>
using namespace std;

int main()
{

    string s = "";

    if (s.empty() == true)
    {
        cout << "Empty " << s.empty() << endl;
    }

    else
    {
        cout << "Not Empty " << s.empty() << endl;
    }

    return 0;
}